port: 4222
http_port: 8222

jetstream {
  store_dir: "/data/jetstream"
  max_memory_store: 1GB
  max_file_store: 10GB
}

accounts {
  default: {
    jetstream: enabled
  }
}

# Logging
logfile: "/data/jetstream/nats.log"
log_size_limit: 100MB
